<div class="columns is-mobile">
  <div class="column is-half-desktop is-half-tablet is-half-mobile">
    <h2 class="title is-2"><%= name %></h2>
    <img src="<%= image %>" alt="<%= name %>">
    <p>tools:
      <% tools.forEach(tool => { %>
        <span class="tag"><%= tool %></span>
        <% }) %>
      </p>
      <p>Concets:
      <h3 class="subtitle is-5"><%= concept %></h3>
    </p>
  </div>
</div>
